#pragma once

/*
class Task
{
public:
	enum Type 
	{
		Worker,
		Coordinator,
		Controller,
		Idle
	};	
	
protected:
	int m_nRank;
	Type m_type;

public:
	int GetRank(void) { return m_nRank; }
	void SetRank(int p_nRank) { m_nRank = p_nRank; }

	bool IsWorker(void) { return m_type == Worker; } 
	bool IsCoordinator(void) { return m_type == Coordinator; }
	bool IsController(void) { return m_type == Controller; }
	bool IsIdle(void) { return m_type == Idle; }
};
*/