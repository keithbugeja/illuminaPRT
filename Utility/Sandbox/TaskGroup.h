#pragma once
#include "Task.h"
/*
class TaskGroup
{
protected:
	std::vector<Task*> m_taskList;

	int m_nGroupId,
		m_nCoordinator,
		m_nController;
	
public:
	int GetGroupId(void) const { return m_nGroupId; }
	int GetCoordinator(void) const { return m_nCoordinator; }
	int GetController(void) const { return m_nController; }

	void AddTask(Task *p_pTask);
	void RemoveTask(Task *p_pTask);
	void RemoteTask(int p_nRank);
	Task* GetTask(int p_nRank);

	void AddTasks(std::vector<Task*> p_pTaskList);
};
*/